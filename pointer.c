#include <stdio.h>

/**
 * data types - determine the size of the variable types
 *
 * Return is always 0
 */
int main()
{
	int n;
	
	printf("Size of data typee char is %d bytes\n", sizeof(char));
	printf("Size of data type int is %d bytes\n", sizeof(int));
	printf("Size of data type float is %d bytes\n", sizeof(float));
	printf("Size of variable n is %d bytes\n", sizeof(n));
	return (0);
}



